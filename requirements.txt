# Core runtime for Phases 0-2 and 4-8 on CPU.
# GPU-only extras for Phase 3 live in requirements-gpu.txt.

# --- config, validation, utilities ---
pydantic>=2.7,<3
PyYAML>=6.0
python-dotenv>=1.0
rich>=13.7
tqdm>=4.66
psutil>=5.9
joblib>=1.4

# --- data ---
numpy>=1.26,<2.2
pandas>=2.2
pyarrow>=16.0
datasets>=2.19
scikit-learn>=1.5

# --- modelling ---
# CPU build of torch. On a CUDA machine, install the matching cuXXX wheel instead.
torch>=2.3
transformers>=4.44
sentence-transformers>=3.0
lightgbm>=4.3

# --- RL ---
gymnasium>=0.29

# --- serving and UI ---
fastapi>=0.111
uvicorn[standard]>=0.30
sse-starlette>=2.1
streamlit>=1.36
httpx>=0.27

# --- plots and tracking ---
matplotlib>=3.9
seaborn>=0.13

# --- dev ---
pytest>=8.2
pytest-cov>=5.0
ruff>=0.5
